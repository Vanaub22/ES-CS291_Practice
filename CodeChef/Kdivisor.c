//To Choose a positive integer K such that K has exactly 4 divisors and K is a divisor of N.
//Change N to N/K and then find the minimum number of divisors possible of the resulting number N.
#include<stdio.h>
void main()
{
    int test,n,x,k;
    printf("\nEnter the number of test cases: ");
    scanf("%d",&test);
    while(test>0)
    {
        test--;
    }
}